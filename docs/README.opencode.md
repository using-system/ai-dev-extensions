# ai-dev-extensions for OpenCode

## Installation

Add to your `opencode.json`:

```json
{
  "plugin": ["ai-dev-extensions@git+https://github.com/using-system/ai-dev-extensions.git"]
}
```

Restart OpenCode to load the skills.

## Updating

OpenCode auto-updates git-based plugins on restart.
